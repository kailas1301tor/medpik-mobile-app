enum LoaderState {
  loaded,
  loading,
  error,
  networkError,
  noData,
  serverError,
  noSearchData,
}

enum UserAccountStatus {
  active,
  suspended,
}

enum OrderStatus {
  placed,
  confirmed,
  packed,
  outForDelivery,
  delivered,
  cancelled,
}
